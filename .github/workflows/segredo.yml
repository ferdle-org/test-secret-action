name: 'Segredo'
on:
  workflow_dispatch: 

jobs:
  segredo:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.SUPERSECRET }}
        run: |
          echo "$SUPER_SECRET"
